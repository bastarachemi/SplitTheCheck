<!DOCTYPE html>
<html class="h-100">
  <head>
    <title>SplitTheCheck</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <style>
      @import url('https://fonts.googleapis.com/css2?family=Nunito+Sans:wght@400;600&family=Playfair+Display:wght@500;700&display=swap');
    </style>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body id="bootstrap-overrides" class="d-flex flex-column h-100">
    <header class="mb-0 d-flex justify-content-between px-4">
      <h1 class="text-center">Split The <span class="orange-text">Check</span></h1>
      <div class="d-flex align-items-center">
        <div class="list-group list-group-horizontal">
          <% if user_signed_in? %>
            <a class="list-group-item dropdown-toggle" href="#" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false"><%= current_user.email %></a>
            <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
              <li><%= link_to 'View Profile', user_profile_path, class: "dropdown-item" %></li>
            </ul>
            <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: "list-group-item" %>
          <% else %>
            <%= link_to 'Log In', new_user_session_path, class: "list-group-item" %>
            <%= link_to 'Sign Up', new_user_registration_path, class: "list-group-item" %>
        <% end %>
        </div>
      </div>
    </header>
    <main class='mt-0 <%= controller.controller_name %>'>
      <% if notice %>
        <p class="notice text-center my-2"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert text-center my-2"><%= alert %></p>
      <% end %>
      <%= yield %>
    </main>
    <footer class="footer p-4">
      <small>Last Updated: 04/27/2022</small>
    </footer>
  </body>
</html>
